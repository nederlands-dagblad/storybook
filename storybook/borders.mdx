import {Meta} from '@storybook/addon-docs/blocks';
import tailwindConfig from '../tailwind.config.js';

<Meta title="Tokens/Borders" />

# Borders

## Border-width

<table>
    <thead>
    <tr>
        <th>Variable</th>
        <th>Value</th>
        <th>Example</th>
    </tr>
    </thead>
    <tbody>
    {Object.entries(tailwindConfig.theme.extend.borderWidth).map(([key, value]) => (
        <tr key={key}>
            <td>border-width-{key}</td>
            <td>{value}</td>
            <td>
                <div
                    style={{
                        background: 'var(--background-accent-gray, #F2F2F2)',
                        width: 'var(--spacing-xl, 80px)',
                        height: 'var(--spacing-xl, 80px)',
                        border: `${value} solid #000000`,
                        borderRadius: 'var(--border-radius-s, 8px)',
                        margin: '0 auto'
                    }}
                />
            </td>
        </tr>
    ))}
    </tbody>
</table>

## Border-radius

<table>
    <thead>
    <tr>
        <th>Variable</th>
        <th>Value</th>
        <th>Example</th>
    </tr>
    </thead>
    <tbody>
    {Object.entries(tailwindConfig.theme.extend.borderRadius).map(([key, value]) => (
        <tr key={key}>
            <td>border-radius-{key}</td>
            <td>{value}</td>
            <td>
                <div
                    style={{
                        background: 'var(--background-accent-gray, #F2F2F2)',
                        width: '145px',
                        height: '60px',
                        border: '1px solid var(--border-default, #000000)',
                        borderRadius: value,
                        margin: '0 auto'
                    }}
                />
            </td>
        </tr>
    ))}
    </tbody>
</table>

